# Claude Code Rules

## 작업 규칙

- 코드/파일 수정 전에 변경 대상과 변경 계획을 먼저 설명한다.
- 사용자 승인 없이 코드나 문서를 수정하지 않는다.
- 한 번에 하나의 기능만 작업한다.
- 기존 기능을 임의로 삭제하지 않는다.
- 🔴 **이 폴더는 아직 git 저장소가 아니다**(2026-09-17 사용자 결정, README
  「정해진 것」). `git init` 을 하지 않는다. `git add`·`commit` 도 하지 않는다.
  저장소로 만들 시점은 사람이 정한다. 아래 푸시 규칙은 그때부터 적용된다.
- Git Push 전에 사용자 승인을 받는다. 푸시 직전에 **무엇이 올라가는지 확인해
  보고한다** — 올라갈 커밋 목록과 `git diff --stat origin/main..main` 의 파일 목록.
  우리 것이 아닌 파일이 섞여 있으면 **올리지 말고 먼저 알린다**.
- AGENTS.md 는 사용자가 명시적으로 승인하지 않는 한 git add/staging 대상에서 제외한다.
- 🔴 **이웃 저장소를 고치지 않는다.** 같은 폴더 아래 RF_Service_System · dss-auth ·
  dss-home · njlee · synergy-attendance · dss-deploy 가 있고, 거기서 일하는 다른
  세션이 있다. **읽기만** 한다(본뜰 것을 보러). 그쪽에 손댈 일이 생기면 직접 하지
  말고 보고한다.

## 작업 효율 규칙 (토큰 절감)

- 작업 범위는 위험도에 따라 조정한다:
  - **LOW** (독립적 로직, 소규모 UI, 읽기 쪽 변경, 테스트 전용 변경): 관련 파일만
    확인, 타겟 테스트만 실행, 전체 저장소 감사 생략.
  - **MEDIUM** (서버 mutation, 인가, 트랜잭션, 워크플로 의미 변경): mutation +
    직접 관련된 schema/types/auth/tests만 확인. 구체적 의존성이 확인될 때만 범위를 넓힌다.
  - **HIGH** (마이그레이션, 마이그레이션 적용, 파괴적 DB 작업, 실 데이터 변경,
    권한 확장, 배포/보안, staging/commit 경계): 명시적 preflight, 작업 직전 실시간
    검증, 불일치 시 STOP, 작업 후 결과 재확인.
- 저장소를 기본적으로 광범위하게 스캔하지 않는다. 시작점은 현재 작업, HEAD,
  git diff, 직접 관련 파일/테스트, CLAUDE.md.
- 이미 검토·테스트·커밋된 동작은 재감사하지 않는다.
- 매 작은 수정마다 전체 테스트를 재실행하지 않는다: 변경 → 타겟 확인 → 다음 변경,
  일관된 단위 완료 시 `tsc --noEmit` + `lint` + 필요한 회귀 1회.
- 이 규칙은 데이터 안전, 인가 경계, 마이그레이션 절차, 승인 게이트를 약화시키지
  않는다 — 위험한 작업(마이그레이션 적용, 실 데이터 변경, staging, commit, push,
  배포, 권한 확장, 대량 삭제)은 여전히 명시적 사용자 승인이 필요하다.

## DB/데이터 규칙

- DB 스키마 변경 전 영향 범위와 Migration 계획을 먼저 보고한다.
- 기존 데이터 삭제 또는 초기화 명령을 실행하지 않는다.
- 위 삭제 금지 규칙은 Claude가 임의로 실행하는 작업에 적용된다. 문서화된 정책에
  따른 자동화 절차(보관기간 만료 자동 삭제 등)는 별도로 취급한다.
- Mock Data와 실제 운영 데이터를 명확히 분리한다.
- 🔴 공용 PostgreSQL 인스턴스(`dss-pg-app`, 5442)에는 A/S·계측기의 **실제 운영
  자료**가 들어 있다. 거기에 롤이나 DB를 만드는 일은 사람이 한다
  (docker-compose.yml 머리말에 절차만 적어 두었다).
- 🔴 포털(dss-auth) DB에 이 시스템을 등록하는 것도 사람이 한다. Claude는 절차만 적는다.

## 배포 규칙

- 운영 배포는 Claude가 직접 수행하지 않는다.
- Claude는 배포 절차와 명령어만 작성한다.
- 테스트 서버 검증 및 관리자 승인 후에만 운영 배포 단계를 안내한다.

## 보안 규칙

- .env 내용을 출력하지 않는다.
- 실제 고객 정보, 회로도, 펌웨어, 비밀번호, API Key를 외부 서비스로 전송하지 않는다.
- 통합 로그인 클라이언트 시크릿을 채팅·이메일로 옮기지 않는다. `.env.local`에만 둔다.

---

# DSS 개선요청

사내 시스템과 일하는 방식에 대한 **개선 요청을 모으는 사내 웹사이트**다.
독립된 사이트이고, 사내 통합 로그인(dss-auth)으로 들어온다.

| 문서 | 내용 |
|---|---|
| [README.md](./README.md) | 실행 방법, 포털 등록 절차, 주소가 바뀌었을 때 |

---

## 지금 어디까지 됐나

**1차 (뼈대와 통합 로그인)** — Next.js + TypeScript + Tailwind + Drizzle 뼈대,
통합 로그인 연동, 로그인하면 빈 화면이 뜬다. 개선요청 기능은 아직 없다.

### 다음에 할 일

| | 작업 |
|---|---|
| 등록 | 포털(dss-auth)에 이 시스템을 등록한다 — README "포털에 등록하기" (사람이 한다) |
| DB | 개발 DB를 띄우고 `npm run db:migrate` (첫 마이그레이션 적용은 사용자 승인 후) |
| 기능 | 개선요청 표와 화면 — 목록 · 작성 · 상태 옮기기 |

---

## 이 프로젝트에서 지켜야 할 것

- **로그인을 만들지 않는다.** dss-auth 포털에 OIDC로 위임한다. 포털과 이야기하는
  코드는 `src/lib/auth/oidc.ts` **한 곳에만** 있다. 화면·서버 액션·라우트 어디에서도
  포털 주소나 토큰 형식을 알지 못하게 한다.
- **권한은 클라이언트가 보낸 값으로 판정하지 않는다.** 역할은 포털이 ID 토큰에
  실어 보낸 `role` 클레임에서만 온다. 모르는 값이 오면 **로그인을 거절한다**
  (`src/lib/auth/sso-login.ts` 의 `decideRole` 주석에 이유가 있다).
- **세션 검증은 `(app)/layout.tsx` 한 곳에서 한다.** 다만 데이터를 바꾸는 서버
  액션·API는 **따로 다시 검증한다** — 레이아웃은 화면을 그릴 때만 돈다.
- 🔴 **세션은 서명 토큰이고, 즉시 끊는 일은 `web_users.sessions_valid_from`
  한 칸이 맡는다.** 백채널 로그아웃이 그 선을 올리고(`auth/session.ts` 의
  `revokeSessionsForSubject`), `getSessionUser` 가 매 요청 그 선보다 먼저
  발급된 토큰을 거절한다. **둘 중 하나라도 빼면 포털에서 로그아웃해도 이
  사이트는 계속 열려 있게 된다** — 조용히 깨지는 종류라 더 위험하다.
  같은 이유로 `getSessionUser` 에서 `web_users` 조회를 "성능"으로 걷어내지
  않는다. 근거는 `src/lib/db/schema.ts` 의 「세션 표는 없다」 절.
- **물리 삭제를 하지 않는다.** 사람 표는 소프트 삭제 4칼럼 고정.
- **파일 경로는 상대경로로, 구분자 `/`, 전부 소문자.** 최종 운영은 NAS(Linux) Docker다.
- **PowerShell 스크립트를 만들지 않는다.** 배치는 TypeScript + tsx.
- **마이그레이션 적용·데이터 변경·git push는 사용자 승인 후에** 한다.
- **주석에 「왜 그렇게 했는가」를 적는다.** 이 회사 저장소들의 특징이고, 실제로
  같은 함정을 두 번 밟지 않게 해 준 장치다.

## 알아두면 좋은 것

- **통합 로그인은 주소를 글자 단위로 대조한다.** 와일드카드도 정규화도 없다.
  그래서 `SSO_ISSUER`·`SSO_REDIRECT_URI`를 `auto`로 두고, 포털 등록값은 `{lan}`
  자리표시자를 쓴다. 둘 다 실행 시점에 이 기계의 실제 사내망 주소를 찾는다.
  자세한 근거는 `dss-auth/docs/주소.md`.
- **`src/lib/lan-address.ts`는 dss-auth의 같은 파일과 같은 판정의 복제본이다.**
  저장소가 서로를 참조하지 않아 공유 패키지가 없다. **한쪽을 고치면 나머지도
  고쳐야 한다** — 둘이 다른 주소를 고르면 redirect_uri가 어긋난다.
- **포털 등록은 사람이 한다.** dss-auth 에서
  `npm run client:register -- --client-id dss-improvements …` 로 하고,
  시크릿은 발급할 때 한 번만 보인다. 잃어버리면 `--rotate` 로 재발급뿐이다.
- **세션 쿠키 이름은 `improvements_session`** 이다. 포털의 `dss_sso` 와 겹치지 않게
  일부러 다르게 두었다. 로그인 왕복용 `improvements_sso_tx` 는 `/api/auth/sso`
  경로에만 붙는다.
- **로그인 화면을 거치지 않는다.** 세션이 없으면 `requireSession` 이 곧장
  `/api/auth/sso/start` 로 보낸다. `/login` 은 거절 사유를 보여줄 때만 쓰이고,
  거기서는 자동으로 다시 보내지 않는다(무한 왕복이 된다).
- **포트는 3500** 이다. 이 PC 에서 A/S 3000 · 통합 로그인 3100 · 회사 홈페이지 3200 ·
  계측기 3300 · 시너지 출석부 3400 이 이미 쓴다. 개발 DB 는 5446 (5432·5433·5434·
  5436·5438·5442·5444 가 이미 쓰인다).
- **A/S 시스템에도 「개선 요청」 화면이 있다**
  (`RF_Service_System/src/lib/db/schema/improvement-requests.ts`). 그것은 A/S
  시스템 **안의** 메뉴별 요청이고, 이 사이트는 회사 전체를 받는 별개의 곳이다.
  설계를 정할 때 그쪽 상태 모델(접수 → 진행중 → 해결)을 참고하면 좋다 —
  **읽기만 한다.**
