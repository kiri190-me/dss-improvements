# Codex Rules

## 작업 규칙

- 코드/파일 수정 전에 변경 대상과 변경 계획을 먼저 설명한다.
- 사용자 승인 없이 코드나 문서를 수정하지 않는다.
- 한 번에 하나의 기능만 작업한다.
- 기존 기능을 임의로 삭제하지 않는다.
- Git Push 전에 사용자 승인을 받는다.
- 이 저장소는 이웃 저장소 다섯(RF_Service_System · dss-auth · dss-home · njlee ·
  synergy-attendance · dss-deploy)과 같은 폴더 아래 있다. **그 저장소들은 읽기만
  한다** — 거기서 일하는 다른 세션이 있다.

## DB/데이터 규칙

- DB 스키마 변경 전 영향 범위와 Migration 계획을 먼저 보고한다.
- 기존 데이터 삭제 또는 초기화 명령을 실행하지 않는다.
- 위 삭제 금지 규칙은 Codex가 임의로 실행하는 작업에 적용된다. 문서화된 정책에
  따른 자동화 절차(보관기간 만료 자동 삭제 등)는 별도로 취급한다.
- Mock Data와 실제 운영 데이터를 명확히 분리한다.
- 공용 PostgreSQL 인스턴스(`dss-pg-app`)에는 다른 시스템의 운영 자료가 들어 있다.
  거기에 롤이나 DB를 만드는 일은 사람이 한다.

## 배포 규칙

- 운영 배포는 Codex가 직접 수행하지 않는다.
- Codex는 배포 절차와 명령어만 작성한다.
- 테스트 서버 검증 및 관리자 승인 후에만 운영 배포 단계를 안내한다.

## 보안 규칙

- .env 내용을 출력하지 않는다.
- 실제 고객 정보, 회로도, 펌웨어, 비밀번호, API Key를 외부 서비스로 전송하지 않는다.
- 통합 로그인 클라이언트 시크릿을 채팅·이메일로 옮기지 않는다. `.env.local`에만 둔다.
